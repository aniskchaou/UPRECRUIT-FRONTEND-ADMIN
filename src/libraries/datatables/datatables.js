// All required scripts (jQuery, DataTables, Bootstrap) are loaded via index.html.
// This function is kept for compatibility but is intentionally a no-op to
// prevent duplicate CDN script injection that caused DataTable conflicts.
export function LoadJS () {
  // no-op: scripts already loaded in public/index.html
}